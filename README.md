# Yeet Chonker experiment

## Build
```
npm install
npm run bundle
```

## Run
```
npx http-server .

# open new terminal
node server.js
```

* Pop open a browser and head over to localhost:8080
* send `test.mov`
